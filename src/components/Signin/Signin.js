import React from 'react';

const Signin = () => {
    return (
        <div className="text-center,bg-primary text-danger">
            <h2>Welcome to vist our hospital web site.</h2>
            <button onClick="/home">Go Home</button>
        </div>
    );
};

export default Signin;